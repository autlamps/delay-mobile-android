<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.izaac.delayed">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pages.LoginPage" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pages.createAccount"
            android:screenOrientation="portrait"/>
        <activity android:name=".pages.homePage"
            android:screenOrientation="portrait"/>
        <activity android:name=".pages.RegisterAccount"
            android:screenOrientation="portrait"/>
        <activity android:name=".pages.TripPage"
            android:screenOrientation="portrait"/>
        <activity android:name=".pages.MultiTripPage"
            android:screenOrientation="portrait"/>
        <activity android:name=".pages.DelayListActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".pages.ListDelayDetails"
            android:screenOrientation="portrait"/>

        <service android:name=".models.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:name=".models.MyFirebaseInstance">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>

        <activity android:name=".pages.Subscriptions" />
        <activity android:name=".pages.SubscriptionListActivity"></activity>
    </application>

</manifest>